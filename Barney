
local name = "gosubnowpls1" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.gosubnowpls1.DisplayName = "[👑] Balligusapo"
end
end)

local name = "beat87267" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.beat87267.DisplayName = "[🌟] bosslittleman"
end
end)

local name = "demonprowarrior" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.demonprowarrior.DisplayName = "[⭐] demonprowarrior"
end
end)

local name = "nooneaskedher" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.nooneaskedher.DisplayName = "[⭐] nooneaskedher"
end
end)

local name = "jojo_2163890" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.jojo_2163890.DisplayName = "[⭐] esg_2009"
end
end)

local name = "gabpogi266" --// Change "playerName" with the name of the person u want to check for that joins

game:GetService'Players'.PlayerAdded:Connect(function(player)
if player.Name == name then
game.Players.gabpogi266.DisplayName = "[⚔️] Rip_pia"
end
end)


local AntiDisplayName = loadstring(game:HttpGet('https://pastebin.com/raw/hCMXnKWa'))(function()
    getgenv().Preferences = {}
end)

Preferences = {
    RetroNaming = false,
    ShowOriginalName = false,
    ApplyToLeaderboard = false,
    IdentifyFriends = {Toggle = true, Identifier = '[Friend]'},
    IdentifyBlocked = {Toggle = true, Identifier = '[Blocked]'},
    IdentifyPremium = {Toggle = true, Identifier = '[Premium]'},
    IdentifyDeveloper = {Toggle = true, Identifier = '[Game Dev]'},
    SpoofLocalPlayer = {Toggle = false, UseRandomName = false, NewName = 'Random Name Lol'},
    Orientation = 'Horizontal'
}
